services:
  - type: web
    name: culinary-bridge-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: cd src && python -m uvicorn api.api:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0